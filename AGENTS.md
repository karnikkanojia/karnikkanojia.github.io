# Project conventions

- Always run the project's intended animations regardless of the operating system or browser motion preference.
- Do not add `prefers-reduced-motion` media queries, reduced-motion hooks, or alternate reduced-motion branches unless the user explicitly reverses this decision.
